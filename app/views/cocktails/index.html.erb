<div class="container" >

  <h1>Cocktails</h1>
  <%= link_to 'Créer votre fameux Cocktail !!', new_cocktail_path, class: "btn btn-success" %>
  <div class="row">
    <% @cocktails.each do |cocktail| %>
      <%= link_to cocktail_path(cocktail) do %>
      <div class="col-md-4" style="margin: 10px;">
        <% if cocktail.photo.attached? %>
          <div class="card-category" style="background-image:
          linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)),
          url('<%= cl_image_path cocktail.photo.key, height: 300, width: 400, crop: :fill %>'); width: 250px; height: 250px;">
            <h5><%= cocktail.name %></h5>
            <ul>
              <% cocktail.ingredients[0..2].each do |ingredient|%>
                <li><%= ingredient.name %></li>
              <% end %>
            </ul>
          </div>
        <% elsif !cocktail.image_url.nil? %>
          <img src="<%= cocktail.image_url %>"
           alt="Card image cap" class="img-responsive" style="width: 250px;height: 250px;">
          <div class="carousel-caption" style="position: absolute; top:8px; left: 24px; font-size: 8px;" >
            <h5><%= cocktail.name %></h5>
            <ul>
              <% cocktail.ingredients[0..2].each do |ingredient|%>
                <li><%= ingredient.name %></li>
              <% end %>
            </ul>
          </div>
        <% end %>
     </div>
     <% end %>
    <% end %>
  </div>


  <h1>Cocktails BIS</h1>
  <div class="row">
    <% @cocktails.each do |cocktail| %>
      <%= link_to cocktail_path(cocktail) do %>
      <div class="col-md-4">
        <% if cocktail.photo.attached? %>
          <div class="card-category" style="background-image:
          linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)),
          url('<%= cl_image_path cocktail.photo.key, height: 300, width: 400, crop: :fill %>'); width: 250px; height: 250px;">
            <h5><%= cocktail.name %></h5>
            <ul>
              <% cocktail.ingredients[0..2].each do |ingredient|%>
                <li><%= ingredient.name %></li>
              <% end %>
            </ul>
          </div>
        <% end %>
     </div>
     <% end %>
    <% end %>
  </div>
</div>
